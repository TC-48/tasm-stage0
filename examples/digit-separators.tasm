set r0, 1'000'000
cmp! r0, 1_000_000
if(ne) nop // unreachable (assuming the assembler and emulator work correctly)
cmp! r0, 1000000
if(ne) nop // unreachable (assuming the assembler and emulator work correctly)

set r1, 0t_102100_012112
cmp! r1, 0n370175
if(ne) nop // unreachable (assuming the assembler and emulator work correctly)

halt

